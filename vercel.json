{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "regions": [
    "fra1"
  ],
  "ignoreCommand": "git rev-parse HEAD^ >/dev/null 2>&1 || exit 1; git diff --name-only HEAD^ HEAD | grep -qvE '\\.md$|^\\.github/|^docs/|^diagnostics/' && exit 1; exit 0"
}
